[Karate Issue](https://github.com/karatelabs/karate/issues/1933)

Start spring boot app
```
mvn spring-boot:run
```

Run tests. If the tests pass try running a few times.

```
 mvn test -Dtest=TestRunner
```
